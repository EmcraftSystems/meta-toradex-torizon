# The userspace firmware helper is no longer implemented by systemd or udev, so
# this fallback is never serviced: firmware the kernel cannot load directly
# fails only after a 60 second timeout.
# CONFIG_FW_LOADER_USER_HELPER_FALLBACK is not set
