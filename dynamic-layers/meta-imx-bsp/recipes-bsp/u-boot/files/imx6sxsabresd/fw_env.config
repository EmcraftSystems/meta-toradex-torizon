# libubootenv has no way to derive this from the board: this machine's
# environment is a raw MMC region, not a partition, and only U-Boot's own
# compiled Kconfig knows its offset and size.
#
# Uboot env device	Device offset	Env. size
/dev/mmcblk3		0xe0000		0x2000
