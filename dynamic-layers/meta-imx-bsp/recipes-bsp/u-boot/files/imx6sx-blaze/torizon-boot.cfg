# scriptaddr, not loadaddr: loadaddr is where the kernel is placed a moment
# later. Both are set by torizon-imx6sx.env.
CONFIG_BOOTCOMMAND="setenv fdtfile imx6sx-blaze.dtb; setenv devtype mmc; setenv devnum 0; ext4load ${devtype} ${devnum}:1 ${scriptaddr} /boot.scr && source ${scriptaddr}"
